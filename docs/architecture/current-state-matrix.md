# Patchwork current-state matrix

Updated: 2026-07-10

This is the authoritative description of what the Patchwork runtime does today. It distinguishes tested contracts and in-memory models from durable or externally integrated behavior. The continuation sequence and exit gates live in `docs/superpowers/plans/2026-07-10-patchwork-continuation-roadmap.md`.

## Maturity labels

| Label | Meaning |
| --- | --- |
| `contract-only` | Types, validators, domain rules, fixtures, or tests exist, but there is no complete runtime path. |
| `fixture-runtime` | A runnable path exists, but its primary state or external behavior is simulated or process-local. |
| `partially-persistent` | A runnable path uses durable storage for part of its behavior, while material state or integration remains simulated or volatile. |
| `externally-integrated` | The runtime talks to its intended external system and has integration coverage, but production operations have not been demonstrated. |
| `production-ready` | External integration, durability, security, deployment, recovery, monitoring, and operational ownership are all demonstrated. |

No subsystem is currently `production-ready`.

## Alpha-critical subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| AT lexicons and validation | `externally-integrated` | `packages/at-lexicons/src/lexicons.ts`, `validators.ts`, and five JSON lexicons load and validate local records. The official client adapter encodes decimal coordinates as AT-compatible integer microdegrees/metres at the PDS boundary. | JSON schemas and fixtures are repository files; disposable live-PDS records have been created, read, updated, and deleted. | `packages/at-lexicons/src/lexicons.test.ts`; `packages/at-client/src/record-client.test.ts`; redacted two-account PDS evidence. | Lexicon publication/registration, compatibility fixtures for every record family, and production operations remain incomplete. |
| Identity and authentication | `partially-persistent` | Non-test API startup constructs the official Node OAuth client. Login can return an opaque authorization URL to the SPA, callback success sets HttpOnly session plus readable CSRF cookies and redirects to the sanitized web destination, and callback failure redirects only a stable error code. `AuthProvider` restores, refreshes, expires, and revokes the cookie-backed session; authenticated shell actions derive the DID from it. | OAuth state and SDK session payloads use AES-256-GCM PostgreSQL stores; opaque browser tokens are stored only as hashes. Browser code receives only DID and expiry and contains no access/refresh-token model. | OAuth adapter/error tests, API auth/store persistence tests, provider/API flow tests, and 35 Chromium accessibility cases cover login and recovery semantics. | A live-account browser callback and deployment OAuth metadata still require the controlled Phase 6 environment. |
| Aid-post record operations | `partially-persistent` | Authenticated `/at/aid-posts` commands restore the caller’s OAuth session and use the official `Agent` create/get/put/delete APIs. Ownership comes from the restored DID; updates, close, and delete require CID compare-and-swap. A direct two-account staging-PDS exercise verified CRUD, compare-and-swap, cross-repository denial, and deletion. | The PDS is the public-record authority; PostgreSQL persists OAuth state and older discovery events. | Command-service tests; official-agent transport tests including wire encoding, cross-DID rejection, invalid records, stale revisions, and PDS errors; redacted live-PDS protocol evidence. | The complete Patchwork API path has not yet been exercised with browser OAuth; ingestion/tombstone integration is also not implemented. |
| Live AT ingestion | `partially-persistent` | The production indexer opens the configured Jetstream WebSocket, filters all Patchwork NSIDs, adapts commit frames, resumes its PostgreSQL cursor, writes normalized aid-post events before acknowledging them, and persists a ten-second heartbeat for quiet-stream readiness. Invalid records are durably quarantined. | Cursor, CID/revision, hashed author identity, approximate geography, searchable fields, status, idempotency ledger, hashed deletion tombstones, bounded dead letters, and heartbeat state survive restart. | Local WebSocket tests cover reconnect, cursor resume, failure redelivery, framing, and shutdown. PostgreSQL tests cover migration replay, create/update/delete, duplicate/stale events, privacy boundaries, dead letters, rebuild equivalence, outage recovery, and runtime heartbeat behavior. | A controlled live-instance lifecycle has not yet proven the Phase 5 exit gate. |
| Discovery map/feed/directory | `partially-persistent` | PostgreSQL mode constructs `PostgresProjectionQueryService`, which reloads the durable aid-post projection snapshot for each map/feed request and applies the shared deterministic filtering/ranking contract. Responses include latest cursor, projection time, heartbeat observation time, and lag. Fixture discovery is reachable only in the test configuration allowed by the authentication startup guard. | Production reads `indexer_aid_post_projections` and `indexer_projection_state`; the older discovery-event table is no longer a production query source. | Real PostgreSQL query tests cover geography, category, urgency, status, search, freshness, stable pagination, deterministic repetition, and response freshness. Readiness unit coverage and startup acceptance prove missing/stale heartbeat rejection. | Directory remains empty in the narrow durable alpha because directory records are outside the aid-post projection slice. Controlled live AT-to-browser evidence remains the Phase 5 exit gate. |
| Minimal request lifecycle | `partially-persistent` | In PostgreSQL mode, transition, query, assignment, acceptance, decline, handoff, timeout, and confirmed-record deletion paths use `PostgresLifecycleRepository`; status, actor role, ordered timeline, assignment and handoff metadata, and audit records survive restart. Confirmed PDS deletion removes the workflow and cascading fulfillment data while retaining a minimal replay-safe audit marker. PostgreSQL roles are resolved by authenticated DID on every request and default safely to `user`. An author-authenticated reconciliation command derives public status from private workflow state, persists `pending`/`failed`/`synced` state with stable failure codes, writes through PDS compare-and-swap, and persists returned status/CID evidence. | PostgreSQL tests demonstrate restart survival, role resolution, authenticated HTTP elevation, public-sync state and checkpoint idempotency, duplicate-command suppression, rollback, timeline reconstruction, deletion reconciliation, atomic assignment, timeout, and handoff operations, and one audit event per command. | `aid-post-command-service.test.ts`; `lifecycle-service.test.ts`; `durable-core-services.test.ts`; `core-operational-state.test.ts`; HTTP/PostgreSQL and direct-service integration suites. | Automatic repository-event reconciliation and browser UX for public-sync partial failure remain Task 3.2. Role provisioning is operator-only and requires an audited database session during alpha. |
| Blocks and reports | `partially-persistent` | Authenticated browser controls call method-aware `POST /blocks` and `POST /reports` routes. The extracted durable HTTP handler restores the session, derives the actor DID and platform role, applies idempotency, and calls PostgreSQL-backed services; hostile actor fields are ignored. Older chat safety compatibility routes remain deferred and unavailable in the production alpha UI. | Private rows carry retention metadata; deletion soft-deletes blocks and clears private report details. | Browser report/block cases, API-client identity-boundary tests, `durable-core-services.test.ts`, PostgreSQL repository tests, and a real HTTP/PostgreSQL test prove session-derived durable actors. | Cross-request enforcement in discovery/chat, compatibility-route removal, and account-deletion orchestration remain. |
| Moderation queue and audit | `partially-persistent` | Production startup requires PostgreSQL, verifies the durable schema before listening, and routes queue and policy operations through PostgreSQL stores. Mutations are method-aware JSON commands authenticated with an internal service bearer token; the API derives the actor from its authenticated session. | Queue state, leases, retry metadata, policy state, immutable audit history, and idempotent command responses survive restart. Fixture stores are restricted to explicit test/development construction. | PostgreSQL integration proves migration replay, concurrent claims, retry timing, terminal exclusion, acknowledgement ownership, lease recovery, atomic policy updates, restart audit history, service-auth rejection, and session-derived actors. | Private case-note policy and operator-facing browser workflow remain later alpha work; public exposure of the worker is prohibited. |
| API transport and authorization | `partially-persistent` | Phase 4’s narrow production surface is method-aware, authenticated, authorized, CSRF-protected, bounded, and free of URL mutations. Domain mutations require an `Idempotency-Key`; the edge overwrites hostile body command IDs and the PostgreSQL ledger serializes delivery and replays responses. AT create hashes the key into a deterministic PDS record key, closing the external-effect crash window. Blocks, reports, lifecycle, assignments, handoffs, AT updates/deletes, and moderation commands all execute through the ledger. | Sessions, roles, alpha private state, moderation/audit state, and idempotency responses are PostgreSQL-backed; limiter state intentionally resets per process. | PostgreSQL HTTP replay proves one lifecycle effect across restart; ledger integration proves concurrent execute-once and conflict rejection; AT-client tests prove deterministic create uses `putRecord`; the web client test proves a generated header. | Phase 4 exit gate is satisfied. Multi-replica rate limits eventually need a shared backend, but that is not required for the single-replica alpha. |
| Web alpha journey | `partially-persistent` | The cookie-only authenticated shell creates AT records without accepting browser authorship, discovers durable projection records with their current CID, reports/blocks other authors through private durable commands, and closes/deletes owner records using CID compare-and-swap. Lifecycle transitions serialize no browser actor/role and use the server-returned principal in local presentation state. Deferred expansion routes render an alpha-scope notice instead of simulated forms. | Auth identity is durable server state; discovery comes from PostgreSQL projections; record mutations use the PDS; reports and blocks use PostgreSQL. Demo records load only in explicit local fixture development builds. | 203 retained web tests, 24 PostgreSQL/HTTP integration cases, and 39 passing Chromium cases cover local behavior. The real two-account Chromium spec is present but skipped without authorized external state. | Execute the real spec through OAuth, live PDS ingestion, projection discovery, safety actions, close, and deletion to satisfy Task 6.3 and the Phase 6 exit gate. |

## Supporting and expansion subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| Chat and message routing | `fixture-runtime` | `createFixtureChatService()` is always used by API startup; deterministic routing and transport-capability decisions exist. | Conversations, capabilities, messages, and safety signals are in memory. | Shared messaging, API chat, and web chat tests. | No live transport, durable history, authenticated participants, pagination, delivery receipts, or restart recovery. |
| Settings and privacy controls | `fixture-runtime` | API startup uses `createFixtureSettingsService()`; the web has a substantial settings UX. | Settings and audit entries are held in a process-local map. | Expansion prototype tests removed; source is typechecked only. | No durable settings, real session ownership, export pipeline, or account deactivation workflow. |
| Volunteer profiles | `fixture-runtime` | API startup uses `createFixtureVolunteerService()`; shared validation and web onboarding exist. | Profiles and checkpoints are in memory and use example DIDs. | Shared and web volunteer-onboarding tests. | No AT record persistence or private operational store, authenticated profile ownership, or durable verification checkpoints. |
| Verification and appeals | `fixture-runtime` | API startup uses `createFixtureVerificationService()` with tier, audit, and appeal behavior. | Verification state, evidence metadata, audits, and appeals are in memory. | Expansion prototype tests removed; source is typechecked only. | No reviewer workflow backed by durable private storage, evidence handling, expiry jobs, or notification integration. |
| Attachments | `fixture-runtime` | `AttachmentService` provides upload metadata and simulated moderation scanning. | Files and scan state live in memory; no object store is configured. | `services/api/src/aid-post-service.test.ts`. | No blob/object storage, real malware/media scanning, signed access, AT blob reference, deletion, or retention enforcement. |
| Inbox | `fixture-runtime` | `createInboxService()` builds aggregation and action behavior for API use. | Inbox items and counters are process-local. | Expansion prototype tests removed; source is typechecked only. | No durable event source, authenticated per-user query, read-state persistence, or integrated real chat/request data. |
| Notifications | `fixture-runtime` | Notification contracts and `NotificationService` model preferences, attempts, deduplication, and channels. | Notification and delivery-attempt state is held in memory; API startup does not construct the service. | Expansion prototype tests removed; source is typechecked only. | No durable outbox/queue, provider adapters, credentials, retries across restart, or production delivery. |
| Scheduling | `fixture-runtime` | `SchedulingService` models availability, shifts, conflicts, reminders, and no-show behavior. | All schedules and assignments are in memory; the service is not wired into API startup. | Expansion prototype tests removed; source is typechecked only. | No durable calendar, authenticated API, notification outbox, timezone operations, or external calendar integration. |
| Groups | `fixture-runtime` | `GroupService` models membership, channels, request-linked rooms, and moderation; web group view models exist. | Groups, memberships, messages, and moderation state are in memory; API startup does not construct the service. | Expansion prototype tests removed; source is typechecked only. | No durable group runtime, real messaging transport, authenticated membership boundary, or integrated UI/API journey. |
| Organizations and resource partners | `fixture-runtime` | `createOrgPortalService()` is wired into API startup and models organizations, members, directory records, and metrics. | Organization and membership state is process-local; generated `did:org:*` values are not real DIDs. | Expansion prototype tests removed; source is typechecked only. | No real organization identity, durable role membership, partner authentication, or live service-status integration. |
| Feedback and outcomes | `fixture-runtime` | `createFeedbackService()` is wired into API startup and validates feedback and summaries. | Feedback is held in memory. | Expansion prototype tests removed; the retained direct-service lifecycle suite touches the happy-path feedback loop. | No durable participant authorization, lifecycle linkage, abuse handling, retention policy, or analytical pipeline. |
| Reputation | `fixture-runtime` | `createReputationService()` is wired into API startup and computes explainable scores from local signals. | Account metadata, handoffs, and feedback signals are in memory. | Expansion prototype tests removed; source is typechecked only. | No durable trustworthy signal source, manipulation controls at system boundaries, appeal process, or pilot validation. |
| Matching | `fixture-runtime` | Shared matching contracts and `MatchingService` compute ranked candidates and explanations. | Requests, candidates, preferences, overrides, and feedback are in memory; API startup does not construct the service. | Expansion prototype tests removed; source is typechecked only. | No durable source data, authenticated operator workflow, integrated runtime endpoint, or fairness evaluation on pilot data. |
| Offline synchronization/PWA | `contract-only` | Shared and web modules model queued operations, conflicts, retry states, and offline UX. | Queues are model objects; no service worker, browser persistence, or production sync protocol is present. | Expansion prototype tests removed; source is typechecked only. | No installable PWA, IndexedDB queue, background sync, authenticated replay, or conflict integration. |
| Native mobile | `contract-only` | `apps/mobile` contains TypeScript API, navigation, offline-sync, and push-notification models. | No native application storage or runtime exists. | Prototype tests removed; source is typechecked only. | No React Native/Expo/iOS/Android UI, device auth, push provider, native build, or store-release pipeline. |
| Multi-region tenancy | `contract-only` | Shared multi-region contracts and API `TenantService` model routing and tenant boundaries. | Tenant state is in memory; no regional infrastructure or replicated datastore is connected. | Expansion prototype tests removed; source is typechecked only. | No deployed regions, residency enforcement, failover routing, replication, or recovery evidence. |
| External connectors | `fixture-runtime` | Connector framework, 311 adapter, and crisis-line adapter simulate initialization, records, health, and failures. | Connector configuration and records are in memory; endpoints are not exercised as real partner integrations. | Expansion prototype tests removed; source is typechecked only. | No partner credentials, durable delivery/outbox, webhook verification, real external contract tests, or operator ownership. |
| Localization | `fixture-runtime` | The web initializes i18next with English and Spanish resources and locale-aware formatting helpers. | Translation resources are bundled files. | 33 web i18n tests. | No translation review workflow or complete live-browser locale acceptance; this is usable prototype behavior, not a production localization program. |
| Accessibility | `fixture-runtime` | Semantic components, focus helpers, announcers, reduced-motion styles, and route-level Playwright specifications exist. | Not applicable to server persistence. | 60 web accessibility unit tests and 33 Playwright cases. | Browser cases were not executable in the 2026-07-10 review until the matching Playwright browser binary is installed; no independent WCAG audit evidence exists. |
| Deployment and operations | `contract-only` | Dockerfiles, Compose files, CI image builds, runbooks, metrics helpers, backup scripts, and staged rollout models exist. | PostgreSQL is the only durable service in the main topology; indexer and moderation persistence are not fully wired. | Config, versioning, and SLI tests remain; staging/progressive/load fixture suites were removed. | CI staging steps build and print readiness but do not publish/deploy artifacts; no current real staging, restore, rollback, alert, or game-day evidence is committed. |

## Confirmed reusable assets

- The five lexicons, their validators, and valid/invalid fixtures.
- Deterministic normalization, tombstone, discovery, ranking, privacy-redaction, and authorization domain logic.
- The PostgreSQL discovery-event slice and migration harness as starting patterns, not a complete schema.
- React design-system primitives, route shell, settings and discovery UX, accessibility helpers, and localization framework.
- The unit and contract test corpus as an executable specification, provided test names are not treated as proof of external integration.
- Docker build stages, configuration validation, metrics formats, and operational runbooks as scaffolding to be verified against a real staging system.

## Data-loss boundaries

A process restart preserves OAuth/session state, lifecycle state and audit, blocks, reports, moderation queues and audit, HTTP idempotency responses, public-sync state, durable aid-post projections, deletion tombstones, dead letters, and the live indexer cursor when their PostgreSQL runtimes are configured. It still loses the older in-process index and all material state in fixture-only expansion subsystems such as chat, settings, volunteer, verification, attachments, inbox, scheduling, groups, organizations, reputation, matching, tenants, and connectors.

## Verification interpretation

The database-enabled full repository gate passes 829 tests. The retained suite includes local WebSocket ingestion, durable projection CIDs, PostgreSQL, authenticated safety HTTP, cookie-only authentication, production data-mode enforcement, and browser-adjacent coverage; 39 Chromium cases pass with one externally gated real-journey case skipped, and nine direct service-integration cases pass separately. Passing local tests still do not substitute for live AT or phase-exit evidence.
