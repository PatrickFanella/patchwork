# Patchwork current-state matrix

Updated: 2026-07-10

This is the authoritative description of what the Patchwork runtime does today. It distinguishes tested contracts and in-memory models from durable or externally integrated behavior. The continuation sequence and exit gates live in `docs/superpowers/plans/2026-07-10-patchwork-continuation-roadmap.md`.

## Maturity labels

| Label | Meaning |
| --- | --- |
| `contract-only` | Types, validators, domain rules, fixtures, or tests exist, but there is no complete runtime path. |
| `fixture-runtime` | A runnable path exists, but its primary state or external behavior is simulated or process-local. |
| `partially-persistent` | A runnable path uses durable storage for part of its behavior, while material state or integration remains simulated or volatile. |
| `externally-integrated` | The runtime talks to its intended external system and has integration coverage, but production operations have not been demonstrated. |
| `production-ready` | External integration, durability, security, deployment, recovery, monitoring, and operational ownership are all demonstrated. |

No subsystem is currently `externally-integrated` or `production-ready`.

## Alpha-critical subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| AT lexicons and validation | `contract-only` | `packages/at-lexicons/src/lexicons.ts`, `validators.ts`, and five JSON lexicons load and validate local records. `packages/at-client` now validates aid posts at the official SDK boundary. | JSON schemas and fixtures are repository files; there is no registered or live repository interaction. | `packages/at-lexicons/src/lexicons.test.ts`; `packages/at-client/src/record-client.test.ts`; phase-8 schema tests. | The official client adapter is not yet wired into API startup or proven against a live PDS. |
| Identity and authentication | `fixture-runtime` | `services/api/src/index.ts` always constructs `createFixtureAuthService()`; `packages/shared/src/identity.ts` supplies `createInMemoryIdentityProvider()`. `packages/at-client` wraps the official Node OAuth client but is not yet consumed by the API. | Sessions live in an API-process `Map`; generated mock tokens disappear on restart. | `packages/shared/src/identity.test.ts`; `services/api/src/auth-service.test.ts`; `packages/at-client/src/oauth-client.test.ts`; web auth view-model tests. | The API lacks real AT OAuth wiring, durable secure session storage, a cookie boundary, and live-account integration. The active mock provider does not validate passwords. |
| Aid-post record operations | `partially-persistent` | `services/api/src/aid-post-service.ts` validates and creates local aid-post events; PostgreSQL mode inserts discovery rows. `packages/at-client` now wraps official `Agent` create/get/put/delete calls with ownership and CID compare-and-swap enforcement, but the API does not use it. | PostgreSQL persists discovery-event rows; it is not the user’s AT repository and does not provide real record revisions. | `packages/shared/src/records.test.ts`; `services/api/src/aid-post-service.test.ts`; `packages/at-client/src/record-client.test.ts`; database seed/query tests. | No API-to-PDS runtime integration, live authenticated-account proof, or live tombstone flow. |
| Live AT ingestion | `partially-persistent` | `services/indexer/src/index.ts` can create an `IndexerPipeline` and load a PostgreSQL checkpoint; `pipeline.ts` accepts arrays passed by callers. | Only the cursor can persist. Normalized indexes remain in process memory. | `packages/shared/src/firehose.test.ts`; indexer phase-3, phase-8, and checkpoint tests. | The runtime never opens `INDEXER_FIREHOSE_URL`, reconnects, resumes a live stream, or stores durable projections/dead letters. |
| Discovery map/feed/directory | `partially-persistent` | `services/api/src/query-service.ts` selects fixture or PostgreSQL query implementations; the web calls those routes with local fallback data. | PostgreSQL persists discovery events in API database mode; fixture mode and indexer indexes are in memory. | `packages/shared/src/discovery.test.ts`; `query-service.postgres.test.ts`; web discovery tests. | Production still permits fixture-oriented paths; data does not originate from live AT ingestion and freshness/lag is not enforced. |
| Minimal request lifecycle | `fixture-runtime` | `services/api/src/lifecycle-service.ts` implements transitions, assignment, handoff, authorization checks, and audit-shaped timelines. | All lifecycle maps and timelines are process-local. | `lifecycle-service.test.ts`; `apps/web/e2e/request-lifecycle.test.ts`. | No transactional repository, restart survival, idempotent command store, authenticated actor boundary, or reconciliation with AT record status. |
| Blocks and reports | `fixture-runtime` | Messaging and moderation models implement safety actions; API moderation/reporting behavior is exercised through local service objects. | State is held in in-memory messaging/moderation stores. | Messaging, moderation, phase-7, feedback, and moderation-worker tests. | No durable block/report repository, cross-request enforcement, authenticated subject ownership, or retention/deletion policy implementation. |
| Moderation queue and audit | `fixture-runtime` | `services/moderation-worker/src/index.ts` explicitly constructs `InMemoryQueueStore`, `InMemoryAuditStore`, and `createFixtureModerationWorkerService()`, even when `DATABASE_URL` exists. | Queue, leases, decisions, and audit history disappear on restart. An initial SQL migration exists but is not wired into runtime stores. | Moderation shared tests; worker durable-queue, service, phase-7, and phase-8 tests. | No PostgreSQL queue/audit implementation, crash-safe leases, durable retry, or production case history. |
| API transport and authorization | `fixture-runtime` | `services/api/src/index.ts` provides a Node HTTP router, body-size checks, CORS, rate limiting, and capability helpers. Many commands and credentials are still represented as URL query parameters. | Rate-limit and session state are process-local; request authorization is not consistently derived from a real authenticated principal. | CORS, rate-limiter, authorization-guard, and authorization contract tests. | No method-aware secure routing across all commands, cookie/CSRF design, durable distributed rate limiting, or end-to-end privilege test with real sessions. |
| Web alpha journey | `fixture-runtime` | `apps/web/src/features/frontend-shell.tsx` renders discovery, posting, volunteer, chat, settings, and later-wave routes and calls selected API functions. | Browser state and imported fixture datasets provide much of the experience; server state is only partly durable. | 432 web unit tests plus Playwright accessibility specifications. | No real AT login or repository journey; production-style API failure may fall back to local datasets; advanced routes are not all wired to runtime services. |

## Supporting and expansion subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| Chat and message routing | `fixture-runtime` | `createFixtureChatService()` is always used by API startup; deterministic routing and transport-capability decisions exist. | Conversations, capabilities, messages, and safety signals are in memory. | Shared messaging and API chat/phase-5 tests; web chat tests. | No live transport, durable history, authenticated participants, pagination, delivery receipts, or restart recovery. |
| Settings and privacy controls | `fixture-runtime` | API startup uses `createFixtureSettingsService()`; the web has a substantial settings UX. | Settings and audit entries are held in a process-local map. | API settings tests; shared settings tests; web settings tests. | No durable settings, real session ownership, export pipeline, or account deactivation workflow. |
| Volunteer profiles | `fixture-runtime` | API startup uses `createFixtureVolunteerService()`; shared validation and web onboarding exist. | Profiles and checkpoints are in memory and use example DIDs. | Shared and web volunteer-onboarding tests; API phase-6 tests. | No AT record persistence or private operational store, authenticated profile ownership, or durable verification checkpoints. |
| Verification and appeals | `fixture-runtime` | API startup uses `createFixtureVerificationService()` with tier, audit, and appeal behavior. | Verification state, evidence metadata, audits, and appeals are in memory. | Shared verification, API verification-service, and web verification tests. | No reviewer workflow backed by durable private storage, evidence handling, expiry jobs, or notification integration. |
| Attachments | `fixture-runtime` | `AttachmentService` provides upload metadata and simulated moderation scanning. | Files and scan state live in memory; no object store is configured. | `services/api/src/aid-post-service.test.ts`. | No blob/object storage, real malware/media scanning, signed access, AT blob reference, deletion, or retention enforcement. |
| Inbox | `fixture-runtime` | `createInboxService()` builds aggregation and action behavior for API use. | Inbox items and counters are process-local. | `inbox-service.test.ts`; web inbox tests. | No durable event source, authenticated per-user query, read-state persistence, or integrated real chat/request data. |
| Notifications | `fixture-runtime` | Notification contracts and `NotificationService` model preferences, attempts, deduplication, and channels. | Notification and delivery-attempt state is held in memory; API startup does not construct the service. | Shared notification and API notification-service tests; web notification tests. | No durable outbox/queue, provider adapters, credentials, retries across restart, or production delivery. |
| Scheduling | `fixture-runtime` | `SchedulingService` models availability, shifts, conflicts, reminders, and no-show behavior. | All schedules and assignments are in memory; the service is not wired into API startup. | Shared scheduling, API scheduling-service, and web scheduling tests. | No durable calendar, authenticated API, notification outbox, timezone operations, or external calendar integration. |
| Groups | `fixture-runtime` | `GroupService` models membership, channels, request-linked rooms, and moderation; web group view models exist. | Groups, memberships, messages, and moderation state are in memory; API startup does not construct the service. | Shared group-coordination, API group-service, and web group tests. | No durable group runtime, real messaging transport, authenticated membership boundary, or integrated UI/API journey. |
| Organizations and resource partners | `fixture-runtime` | `createOrgPortalService()` is wired into API startup and models organizations, members, directory records, and metrics. | Organization and membership state is process-local; generated `did:org:*` values are not real DIDs. | API org-portal and web org-portal tests. | No real organization identity, durable role membership, partner authentication, or live service-status integration. |
| Feedback and outcomes | `fixture-runtime` | `createFeedbackService()` is wired into API startup and validates feedback and summaries. | Feedback is held in memory. | API feedback-service and web feedback tests. | No durable participant authorization, lifecycle linkage, abuse handling, retention policy, or analytical pipeline. |
| Reputation | `fixture-runtime` | `createReputationService()` is wired into API startup and computes explainable scores from local signals. | Account metadata, handoffs, and feedback signals are in memory. | Shared reputation and API reputation-service tests. | No durable trustworthy signal source, manipulation controls at system boundaries, appeal process, or pilot validation. |
| Matching | `fixture-runtime` | Shared matching contracts and `MatchingService` compute ranked candidates and explanations. | Requests, candidates, preferences, overrides, and feedback are in memory; API startup does not construct the service. | Shared matching, API matching-service, and web matching tests. | No durable source data, authenticated operator workflow, integrated runtime endpoint, or fairness evaluation on pilot data. |
| Offline synchronization/PWA | `contract-only` | Shared and web modules model queued operations, conflicts, retry states, and offline UX. | Queues are model objects; no service worker, browser persistence, or production sync protocol is present. | Shared offline-sync and web offline-UX tests. | No installable PWA, IndexedDB queue, background sync, authenticated replay, or conflict integration. |
| Native mobile | `contract-only` | `apps/mobile` contains TypeScript API, navigation, offline-sync, and push-notification models. | No native application storage or runtime exists. | 46 mobile unit tests. | No React Native/Expo/iOS/Android UI, device auth, push provider, native build, or store-release pipeline. |
| Multi-region tenancy | `contract-only` | Shared multi-region contracts and API `TenantService` model routing and tenant boundaries. | Tenant state is in memory; no regional infrastructure or replicated datastore is connected. | Shared multi-region and API tenant-service tests. | No deployed regions, residency enforcement, failover routing, replication, or recovery evidence. |
| External connectors | `fixture-runtime` | Connector framework, 311 adapter, and crisis-line adapter simulate initialization, records, health, and failures. | Connector configuration and records are in memory; endpoints are not exercised as real partner integrations. | Shared integrations, connector-service, community-311, and crisis-line tests. | No partner credentials, durable delivery/outbox, webhook verification, real external contract tests, or operator ownership. |
| Localization | `fixture-runtime` | The web initializes i18next with English and Spanish resources and locale-aware formatting helpers. | Translation resources are bundled files. | 33 web i18n tests. | No translation review workflow or complete live-browser locale acceptance; this is usable prototype behavior, not a production localization program. |
| Accessibility | `fixture-runtime` | Semantic components, focus helpers, announcers, reduced-motion styles, and route-level Playwright specifications exist. | Not applicable to server persistence. | 60 web accessibility unit tests and 33 Playwright cases. | Browser cases were not executable in the 2026-07-10 review until the matching Playwright browser binary is installed; no independent WCAG audit evidence exists. |
| Deployment and operations | `contract-only` | Dockerfiles, Compose files, CI image builds, runbooks, metrics helpers, backup scripts, and staged rollout models exist. | PostgreSQL is the only durable service in the main topology; indexer and moderation persistence are not fully wired. | Config, staging, versioning, progressive-delivery, load, and SLI tests. | CI staging steps build and print readiness but do not publish/deploy artifacts; no current real staging, restore, rollback, alert, or game-day evidence is committed. |

## Confirmed reusable assets

- The five lexicons, their validators, and valid/invalid fixtures.
- Deterministic normalization, tombstone, discovery, ranking, privacy-redaction, and authorization domain logic.
- The PostgreSQL discovery-event slice and migration harness as starting patterns, not a complete schema.
- React design-system primitives, route shell, settings and discovery UX, accessibility helpers, and localization framework.
- The unit and contract test corpus as an executable specification, provided test names are not treated as proof of external integration.
- Docker build stages, configuration validation, metrics formats, and operational runbooks as scaffolding to be verified against a real staging system.

## Data-loss boundaries

A process restart currently loses all material state held by authentication, lifecycle, chat, settings, volunteer, verification, attachments, moderation, inbox, notifications, scheduling, groups, organizations, feedback, reputation, matching, tenants, and connectors. PostgreSQL mode preserves API discovery-event rows and can preserve an indexer cursor when a database URL is supplied, but the indexer’s normalized search store remains in memory and the live stream is not connected.

## Verification interpretation

The repository’s local `npm run check` gate passed 1,945 unit and contract tests on 2026-07-10, and all workspaces built. This is strong evidence that the modeled behavior is internally consistent. It is not evidence of live AT OAuth, repository writes, stream consumption, durable cross-service workflows, real partner integrations, or production operations. Those claims require the continuation roadmap’s phase exit gates.
